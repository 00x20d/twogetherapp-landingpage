services:
  twogether-landing:
    build: .
    container_name: twogether-landing
    restart: unless-stopped
    ports:
      - "127.0.0.1:3002:3002" # only localhost can reach Node, nginx proxies
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3002
